{
  "name": "dsa-question-tracker-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:frontend": "cd frontend && npm start",
    "build:frontend": "cd frontend && npm run build",
    "deploy:frontend": "cd frontend && npm run deploy",
    "start:backend": "cd backend/dsa-tracker-backend && mvn spring-boot:run",
    "build:backend": "cd backend/dsa-tracker-backend && mvn compile"
  }
}
